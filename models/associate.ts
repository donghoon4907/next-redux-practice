export type Associate = {
    // 손보 or 생보
    type: string;
    // 등록번호
    no?: string;
    // 등록보험사
    wcode?: number;
    // 등록일
    indate?: string;
    // 해촉일
    outdate?: string;
    // 지점장
    manager_id?: string;
    //
    qulification?: string;
};
